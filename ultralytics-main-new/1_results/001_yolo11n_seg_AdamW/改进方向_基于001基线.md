# 001 YOLO11n-seg 基线结果分析与改进方向

> 本文档针对 `1_results/001_yolo11n_seg_AdamW/` 这次基线结果，结合**前向传播 vs 标签审计**的实测误差，给出后续改进方向。
> 日期：2026-07-12 ｜ 关联：[`3_研究生/柑橘套袋视觉_难点与创新点分析.md`]、实验脚手架 `train_citrus_seg.py`/`eval_citrus_seg.py`、审计脚本 `vis_pred_vs_gt.py`

---

## 1. 基线配置与结果

- **模型**：`0_orange_yaml/001_yolo11-seg.yaml`（**原版 YOLO11n-seg**，无架构改动，nano）
- **数据**：`200orange_wuxi_seg.yaml` → `data/test`（941 张：train 659 / val 188 / test 94，单类 `orange_immature`）
- **训练**：AdamW，300 轮跑满，imgsz 640，batch 4，lr0 0.001，seed 42

**验证集指标（best @ epoch 240）**：

| | Precision | **Recall** | mAP50 | mAP50-95 |
|---|---|---|---|---|
| Box | 0.914 | **0.737** | 0.822 | 0.671 |
| Mask | 0.900 | **0.737** | 0.814 | **0.644** |

**一句话诊断**：**高精度、低召回**（P≈0.90 / R≈0.74）——模型很保守，报出来的基本对，但**漏掉约 26% 的果**；且 mask 从 mAP50(0.81)→mAP50-95(0.64) **掉 0.17**，说明**掩膜边界质量一般**。训练已收敛（best@240，最后 60 轮 recall 反而略降 0.737→0.718，轻微过拟合），召回天花板卡在 ~0.74。

---

## 2. 前向传播 vs 标签审计（回应"漏标"怀疑）

用 `vis_pred_vs_gt.py` 在 **val 集最密集的 12 张**上跑 best.pt（conf 0.25），逐张对比 GT(绿) / 预测(红) / 疑似漏标(黄)：

**总计：GT=220，预测=156，疑似漏标=11，漏检(GT无预测)=76**

### 结论（明确回答你的怀疑）

1. **漏标确实存在，但是次要问题**：只有 11 个模型可见的漏标候选，集中在少数图（`IMG_20260706_105159` 一张就占 5 个——模型检出了 5 个没标的果，从对比图看确有真果未标）。**建议人工复查这几张**：`IMG_20260706_105159`(5)、`IMG20231120161107_BURST006`(2)、`IMG20231120161204` / `IMG20231120161728` / `IMG_20260706_105119` / `IMG_20260706_104206`(各1)。
   - ⚠️ 注意：模型是在这批标签上训练的，会**继承标注盲区**——它也学会忽略你漏标的果。所以"模型可见的漏标"是**下限**，真实漏标可能更多（尤其小/重遮挡果）。若要彻底排查，需人工过一遍或用更低 conf 再扫。

2. **真正的主要矛盾是"漏检"（低召回）**：模型漏掉 **76/220 = 34%** 的已标注果。两大驱动：
   - **① 小目标 / 宽景图（最狠）**：`IMG_20260706_105149`（GT=23 只预测 8，漏 15）、`IMG_20260706_105623`（GT=22 只预测 4，漏 17）都是**宽景 + 果实极小**的图，640 下小果只有几像素，模型只抓住中间一簇、其余全漏。
   - **② 同色哑光果**：哑光深绿批次（颜色失效、只剩纹理）漏检明显高于光泽黄批次——印证[难点分析]里"颜色崩塌"的判断。

> 一句话：**你怀疑的漏标是有的（次要），但这次基线暴露出的头号问题是"小 + 同色果的漏检"**——而这恰恰是你论文创新点要打的靶心，是好消息。

---

## 3. 误差归因 → 改进方向（按性价比排序）

### 3.1 数据 / 标签层面（先做，最便宜，收益直接）

| 动作 | 依据 | 说明 |
|---|---|---|
| **复查漏标候选图**（上面 6 张） | 审计 11 个候选 | 人工确认后补标；漏标会同时压低召回(训练学会抑制)和精度(验证误判FP) |
| **处理宽景小果图** | 105149/105623 漏检 65~77% | 这类图果实 <8px，640 下近乎不可解。选项：①这类图**提高 imgsz 或切片训练** ②或标注/评测时单列，避免拉低整体指标 |
| **核查 train/val/test 的域与尺度平衡** | 2023光泽 vs 2026哑光、宽景 vs 特写 | 保证三个 split 分布一致，否则指标不可比、结论不稳 |

### 3.2 召回恢复（**核心，直接对应 34% 漏检**）

| 改进 | 打的误差 | 预期 | 对应实验 |
|---|---|---|---|
| **+ P2 小目标头**（stride-4，Segment 覆盖 P2~P5） | 小目标/宽景漏检（#1 驱动） | 召回↑（尤其小果） | **E1** |
| **SFM 频域/纹理模块**（替换选定 C3k2） | 同色哑光漏检（#2 驱动） | 哑光批次召回↑ | **E2** |
| 提高 imgsz（640→960/1280）或切片 | 小目标漏检 | 召回↑但 FPS↓ | 消融变量 |

### 3.3 掩膜边界质量（对应 mAP50→50-95 掉 0.17）

- 边界感知损失 / 更高分辨率 proto / 掩膜细化头。套袋要精确轮廓，这项对下游定位有直接价值。

### 3.4 遮挡 / 同色粘连分离

- 密集簇生的同色果触碰处易糊成一团 → 边界/质心感知机制（见难点分析 A-2）。

---

## 4. 下一步实验清单（承接 E0→E4 阶梯）

> 本 001 run 即等价于 **E0 基线**（原版 YOLO11n-seg）。据上面误差，消融优先级：

1. **E1｜+P2 小目标头**（纯 YAML，仿 `yolov8-p2.yaml`）——先打召回头号驱动（小目标）。
2. **E2｜SFM 频域模块**（SFM 已注册，改 YAML 替换 C3k2）——打同色哑光漏检，是论文技术主创新。
3. **E3｜注意力**（C2PSA/A2C2f 增强）——遮挡/密集。
4. **E4｜组合**（P2 + SFM + 注意力）——最终版。
5. 全程用 `eval_citrus_seg.py` 统一评测（含 test 集），指标写入 `results_summary.csv`，与 E0 对齐。

**每步只改架构、其余超参锁定**（干净消融）。每个改进版训练完，都用 `vis_pred_vs_gt.py` 复查：召回是否在"小果/哑光"上真的回升。

---

## 5. 关联文档

- 难点与创新点：`3_研究生/柑橘套袋视觉_难点与创新点分析.md`
- 完整执行计划：`3_研究生/柑橘套袋视觉_完整研究执行计划.md`
- 审计工具：`ultralytics-main-new/vis_pred_vs_gt.py`（可复用于任意 run 的 best.pt）
- 对比图输出：`1_results/001_yolo11n_seg_AdamW/pred_vs_gt/val/*_cmp.jpg`
