# RF-DETR segmentation family (scripts/train_rfdetr.py / eval_rfdetr.py)
# Install a CUDA-matched torch/torchvision build FIRST; rfdetr pulls torch as a
# dependency and a preinstalled build prevents an unwanted torch upgrade.
# baselines.yaml pins package_version 1.3.0 (RFDETRSegPreview @312).
rfdetr>=1.3,<2
pycocotools>=2.0.7
tqdm>=4.66
PyYAML>=6.0
Pillow>=9.5
numpy>=1.23
