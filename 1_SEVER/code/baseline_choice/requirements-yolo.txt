# YOLO family (run_yolo_baselines.py / scripts/train_yolo.py / eval_yolo.py)
# First install a CUDA-matched torch/torchvision build (pytorch.org selector),
# then the fork:  pip install -e ../ultralytics-main-new
# The fork supplies ultralytics 8.4.x with yolov8n/yolo11n/yolo12n/yolo26n-seg.
pycocotools>=2.0.7
tqdm>=4.66
PyYAML>=6.0
Pillow>=9.5
numpy>=1.23
