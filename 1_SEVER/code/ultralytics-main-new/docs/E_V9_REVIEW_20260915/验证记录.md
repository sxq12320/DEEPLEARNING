# E V9 验证记录

2026-09-15，本地CPU，Python3.9.13、Torch2.8.0，2个数学线程。不是正式GPU实验。

- pytest：`tests/test_citrus_e_v9.py tests/test_citrus_e_v8.py`，69 passed，4项第三方THOP弃用警告。
- Ruff：全部12个本次新增Python文件通过。
- 10份YAML均经官方YOLO入口构建；全组正常/空GT反传、矩形输入、Conv/BN融合、预训练对齐和权重保存/重载检查。
- 新两组控制与旧V8_00/V8_03加载相同权重后的预测逐值一致。
- 掩膜融合的参数变化不会在固定权重下改变检测输出；共享主干训练梯度不在这个等价声明内。
- 00/08/09在小型真实图像fixture上完成1epoch切片混合训练、best/best_mask/best_mask50保存与独立验证。
- 实际执行`RUN_CITRUS_E_V9.main()`，临时设置本地正式数据路径、device=cpu、dry_run=True；十组全部BUILD OK。
  没有把临时CPU/数据参数写回用户服务器入口，入口仍DEVICE=1、DRY_RUN=False、300epoch。
- 参数/FLOPs和smoke输出详见`V9_verification.json`。该JSON内0精度来自只有1epoch/极小样本的功能检查，不是模型性能结论。
- 修复了测试发现的空GT时TAL fg_mask为浮点张量、不能直接取反的问题；正式使用显式bool转换。

完整日志留在`E:/mastercode/_work/citrus_ev9_20260915/`：pytest_final.log、smoke.log、entry.log。
没有覆盖原始数据、已有训练结果或旧系列YAML。

图稿以Matplotlib本地矢量输出，遵循scientific-schematics的版式/可读性规范；未调用付费在线图像服务。
`figures/layout_checks.json`检查文字位于节点内、字号、格式和DPI。图稿已人工视觉核对流程与代码：
继承细节回传标记为neck之后的一遍操作，避免误画成循环控制；新分支只进入mask路径。
未声称通过Gemini在线评分或不存在的自动科研正确性认证。投稿前仍按目标期刊版心审查。

未验证：服务器Torch1.13/CUDA实测延迟、300epoch正式精度、三seed统计与held-out test泛化。
