# V10 回传划分与本地正式划分不同

核对日期：2026-09-17。只读核对；没有重划分、删除或修改数据。

|项目|V10 回传记录|本地 grouped_dedup_20260820|
|---|---:|---:|
|训练原图|676|676|
|训练原图实例|4324|4120|
|验证原图|193|193|
|验证实例|1049|1181|
|验证图像文件名交集|45|45|

两边验证集各有148个独有文件名。图像张数相同不能证明是同一划分。
V10验证193个文件名在本地 split_manifest.csv 中全部可找到，属于本地 train/val/test 的数量分别为123/45/25；
按本地 manifest 对这些193张求和得到1048个实例，与服务器1049仍差1个。
这提示不仅划分可能不同，标注版本/加载策略也需要核对。仅凭名字不能证明像素内容和标签完全一致。

来源：V10_05_structure_pair_seed42/val_loaded_files.txt、loaded_data_summary.json；
本地 data/orange_yolo_grouped_dedup_20260820/audit/split_manifest.csv；本地三份原始txt逐行计数。
本地 train/val/test 实例数4120/1181/589，合计5890。

V10训练回传列表为 source000xxx_viewY.png，原图ID映射没有随本轮结果回传，因此不能据此计算服务器自身train/val组泄漏率。
没有把123张本地train出现在服务器val直接称为服务器内部泄漏，两种划分之间的交叉不是这种证据。
如果25张同名图的内容也相同，本地当前test已有图像在历史服务器验证中用于选模型，不能再声称这个test完全未用于调参。
论文定稿前应核对服务器原图名单/分组manifest和标注版本；必要时另留从未参与调参的独立采集批次。

V11保留用户自己的服务器 DATA 路径，不自动改路径、不增设确认门槛。
V10内部模型比较仍可用：十组回传train/val列表一致。与本地正式数据或其他系列的绝对指标不能直接比较。
本地32图分配探针、几何统计只描述本地数据，不是V10服务器验证集的直接诊断。
