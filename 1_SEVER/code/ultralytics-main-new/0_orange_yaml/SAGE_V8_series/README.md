# SAGE V8

80：与V7的70相同的当前协议对照；81：独立P2候选塔；82：P2/P3/P4预算重分配；
83：82加亮度相位细节stem；84：只改变stem的对照。

模型YAML可通过本仓库 `YOLO(yaml)` 直接构建。批量前台入口：根目录 `RUN_SAGE_V8.py`。
完整依据、固定超参、成本、局限与运行方式：`docs/SAGE_V8_DESIGN.md`。
原系列与数据未删除。新增模型尚无正式精度结果。
