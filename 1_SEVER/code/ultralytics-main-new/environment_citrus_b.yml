name: citrus-b
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.1
  - torchvision
  - pytorch-cuda=12.1
  - pip:
      - -e .
      - pytest
# Deliberately no mamba-ssm, causal-conv1d, selective-scan, MMCV, MMDetection, or custom CUDA operator.
