# 目录说明

本仓库按“当前课题优先、历史内容归档、数据与代码分离”的原则组织。

## 当前课题

- `3_研究生/`：柑橘套袋课题文档。当前研究主线文件保留在该目录顶层。
- `ultralytics-main-new/`：正在使用的 Ultralytics 实例分割代码、模型配置和实验脚本。
- `4_baseline_choice/`：Mask R-CNN、U-Net、MMDetection、RF-DETR 等对比基线。
- `data/`：原始数据、转换后数据和数据处理工具。

## 数据目录

- `data/orange_wuxi/`：原始柑橘图像和 LabelMe JSON 标注，不要直接改名或移动。
- `data/orange_yolo/`：YOLO 格式数据。
- `data/test/`：当前已划分的数据副本，正式实验前需确认其来源和分组划分。
- `data/tools/`：数据转换与合并脚本。
- `data/references/`：数据相关参考文本。
- `data/_archives/`：压缩备份，只用于恢复，不作为训练输入。

## 历史与辅助内容

- `1.coding/`：深度学习学习代码和旧模型实现。
- `2_catoon/`：动画与可视化练习。
- `3_研究生/90_历史资料/`：旧选题、旧调研网页和论文文本摘录。
- `9_archive/`：旧根目录脚本及无效实验输出，不应用于正式论文结果。

## 使用约束

训练配置必须引用明确的数据 YAML，不要引用压缩包或归档目录。正式实验结果统一放入对应工程的结果目录，并使用唯一实验名，避免覆盖。`FWQ` 为服务器文件区，未经明确确认不得修改。
