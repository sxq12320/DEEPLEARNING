# E:\mastercode 目录整理审计（2026-08-24）

## 整理边界

本轮先建立清楚的入口与删除证据。任何可能影响论文复现、数据溯源、已完成实验或个人资料的内容均不自动删除。

## 当前空间构成

| 位置 | 文件数 | 约占用 | 判定 |
|---|---:|---:|---|
| `data/` | 7,767 | 18.40 GB | 原始数据、3 个解压目录和 3 个 ZIP 混存，是最大重复源 |
| `1_SEVER/` | 3,765 | 9.17 GB | 历史代码与结果；其中结果 9.14 GB |
| `.git/` | 540 | 3.10 GB | 历史包含大量已删除学习资料；不能用普通删文件缩小 |
| `ultralytics-main-new/` | 896 | 0.04 GB | 当前正式代码，必须保留 |
| `4_baseline_choice/` | 966 | 0.01 GB | 跨家族基线，必须保留 |

以上容量为盘点时的文件逻辑大小，可能因文件系统压缩/稀疏文件略有差异。

## 必须保留

| 内容 | 原因 |
|---|---|
| `ultralytics-main-new/` | 唯一活跃代码库和 20260824 CitrusTopo-Seg 交付物 |
| `data/orange_yolo_grouped_dedup_20260820/` | 当前唯一正式训练 split |
| `data/orange_wuxi/` | 原始 LabelMe/素材，是所有转换数据的源头 |
| `3_研究生/` | 研究计划、文献和论文证据 |
| `4_baseline_choice/` | 非 YOLO 对比实验实现 |
| `1_SEVER/results/` 中的指标、配置、曲线和关键权重 | 负结果与历史结果同样是论文证据 |
| 当前 Git 未提交改动 | 包含本轮网络与过去用户修改，不能回滚或覆盖 |

## 已确认的重复与清理候选

| 候选 | 可释放 | 核验证据 | 建议 |
|---|---:|---|---|
| `data/_backups/orange_yolo_before_cleaning_20260818_204629/` | 2.775 GB | 与 `data/orange_yolo/` 的 1,936 个同路径文件逐文件 SHA-256 完全一致；仅多出 `tiny_object_audit.csv` | 先迁出该 CSV，再删除整个备份目录 |
| `1_SEVER/results/002_retrain/` 周期检查点 | 约 5.85 GB | 24 个非 best/last 的 `.pth/.pt`；同目录另有 best/last 和最终指标 | 已结束且无需中间 epoch 分析时删除周期 checkpoint，保留 best、last、配置、CSV 和曲线 |
| `data/orange_yolo.zip` | 2.522 GB | 旧 YOLO 解压目录仍在本机 | 无离线灾备需求时删除 |
| `data/orange_wuxi.zip` | 2.379 GB | 原始解压目录仍在本机 | 无离线灾备需求时删除；原始数据最好至少在另一块盘保留一份 |
| `data/orange_yolo_grouped_dedup_20260820.zip` | 2.524 GB | 正式数据解压目录仍在本机 | 服务器上传完成且已有异盘备份后删除本机 ZIP |
| Python/pytest/Ruff 缓存 | 16.56 MB | 1,070 个缓存文件，可自动重建 | **已删除**；共清理 92 个缓存目录 |
| Git 未引用垃圾 | 329.86 MiB | `git count-objects -vH`；含无索引 pack、临时对象和 dangling 对象 | 先提交/备份当前工作，再单独做 Git 恢复点和清理 |

前三类大候选合计最高可释放约 16 GB，但不得在没有异盘备份和用户确认时一次性删除。

## 不应按“看起来旧”删除

- `1_SEVER/results/000_anyothers/`：包含模块正负结果和 PR 曲线，是判断改进是否有效的证据。
- `1_SEVER/archive/code_pre20260824/ultralytics-main-new/`：旧代码已移出服务器上传目录，但仍保留用于解释旧模型 YAML 与结果的对应关系。
- `2_catoon/`、`5_novels/`：与论文无关，但属于独立个人内容，只能在用户明确确认后移动或删除。
- 根目录旧说明书、图片和绘图脚本：体积很小，不值得为省空间冒误删风险；后续可整体归入“历史资料”。
- `weights` 空目录：部分表示失败/未完成 run，可作为运行状态证据，不能在结果清点前批量删。

## 发现的安全问题

Git 当前追踪 `/.codex/auth.json` 和 `/ultralytics-main-new/.env`。盘点仅读取了键名，确认其中分别存在
`OPENAI_API_KEY` 与 `DASHSCOPE_API_KEY`，未读取或输出其值。`.gitignore` 已新增明确规则，但历史中的密钥仍需：

1. 立即在对应平台撤销并重新生成；
2. 从 Git 索引移除本地密钥文件；
3. 在做好仓库镜像备份后，用专用工具清理 Git 历史；
4. 如果仓库曾推送到远端，按“已经泄露”处理，不能只靠删除本地文件。

## 推荐的最终顶层逻辑

```text
E:\mastercode
├─ README.md                         # 唯一入口
├─ ultralytics-main-new\             # 当前网络与批量训练
├─ data\                             # 数据源、正式 split、数据审计
├─ 3_研究生\                         # 研究计划、文献与项目管理
├─ 4_baseline_choice\                # 跨家族 baseline
├─ 1_SEVER\                          # 历史代码/结果，只读证据
├─ 2_catoon\                         # 独立课程旁支
└─ 5_novels\                         # 独立个人写作旁支
```

当前不强行移动这些大目录，避免破坏训练脚本中的绝对路径、Git 历史和服务器同步。逻辑入口通过根 `README.md`
统一，待正式数据与旧结果完成异盘备份后，再执行第二阶段物理精简。

## 本轮已执行

- 重写根 `README.md`，明确唯一活跃代码、唯一正式数据、历史结果和独立旁支。
- 删除 92 个 `__pycache__`、`.pytest_cache`、`.ruff_cache` 或 `.mypy_cache` 目录，共 1,070 个文件、16.56 MB；均可由 Python/pytest/Ruff 自动重建。
- 将本地密钥路径加入 `.gitignore`，并从 Git 索引移除；工作区中的实际文件仍保留。
- 未删除任何数据集、ZIP、模型权重、实验结果、论文资料、旧代码或个人资料。
