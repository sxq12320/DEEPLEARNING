# 模块使用说明

本文件是 `E:\mastercode` 的自定义网络模块总入口。当前已整理的模块为
`C2MANO` 和 `HVIEnhance`，后续新增 StarNet、轻量化 Backbone、Neck 融合模块时，
继续在本文件中增加独立章节，并记录对应的源码、YAML、训练方式和验证结果。

## 目录约束

- 主开发代码：`ultralytics-main-new/`
- 自定义模块实现：`ultralytics-main-new/ultralytics/nn/modules/`
- 模型 YAML：`ultralytics-main-new/0_orange_yaml/`
- 训练脚本：`ultralytics-main-new/train_citrus_seg.py`
- 当前 YOLO 数据集：`data/orange_yolo/`
- `1_SEVER/` 是从服务器原封不动复制的只读快照。本地开发、验证和文档整理均不得修改其中的任何文件。

---

## C2MANO：MANO 多极注意力模块

> 模块文件：`ultralytics-main-new/ultralytics/nn/modules/mano.py`  
> 示例配置：`ultralytics-main-new/0_orange_yaml/004_yolo11-seg-mano.yaml`  
> 适用任务：YOLO11 RGB 柑橘幼果实例分割  
> 来源论文：*Linear Attention with Global Context: A Multipole Attention Mechanism for Vision and Physics*
> （MANO，ICCV-W 2025，arXiv:2507.02748）

### 1. 模块用途

MANO（Multipole Attention Neural Operator）受快速多极子方法启发，通过多尺度特征
和共享窗口注意力建立较大感受野。当前代码将其封装为符合 Ultralytics 解析约定的
`C2MANO`，用于替换 YOLO11 Backbone 末端的 `C2PSA`，形成单变量消融：

- `E0`：原始 `C2PSA`
- `E1`：`C2MANO`

`C2MANO` 的输入输出均为 NCHW 格式，通道数和空间尺寸不变，适合在保持通道数的
位置直接插入。

### 2. 模块结构

```text
C2MANO
└── n × MANOBlock
    ├── MultipoleAttention
    │   └── _WindowMHSA
    └── FFN（1×1 Conv → 2c → 1×1 Conv）
```

核心流程为：输入特征图建立逐级下采样金字塔；每一级使用共享的窗口多头自注意力；
然后通过 V-cycle 从粗到细进行聚合，输出与输入具有相同的 `[B, C, H, W]` 形状。

当前实现针对 YOLO 做了三项适配：

1. 参考实现使用 NHWC，本仓库全程使用 NCHW；
2. 金字塔层数在每次 `forward` 中根据实际特征图尺寸动态计算；
3. 下采样前自动补齐、上采样后精确对齐，兼容 80、40、20 等非 2 的幂尺寸。

### 3. 输入输出与限制

| 项目 | 说明 |
|---|---|
| 输入 | `[B, C, H, W]`，NCHW、浮点特征图 |
| 输出 | `[B, C, H, W]`，通道数和空间尺寸均不变 |
| 通道约束 | `C2MANO` 要求 `c1 == c2` |
| 尺寸约束 | 无需手动满足 2 的幂，内部自动补齐和裁剪 |
| 推荐位置 | Backbone 末端或 Neck 中不改变通道数的位置 |

如果插入 Neck，后续层编号会变化，必须同步检查 YAML 中 `Concat` 和 `Segment` 的
引用索引。

### 4. 参数说明

#### 4.1 `C2MANO`

```python
C2MANO(
    c1, c2, n=1, e=0.5, window_size=4,
    num_heads=None, sampling_rate=2, max_levels=3
)
```

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `c1` | 自动 | 输入通道，由 `parse_model()` 填充 |
| `c2` | 自动 | 输出通道，由 YAML 首参数经 width 缩放得到，必须等于 `c1` |
| `n` | 1 | `MANOBlock` 堆叠次数，由 YAML repeats 和 depth 缩放得到 |
| `e` | 0.5 | 隐藏通道比例，越小越省算力 |
| `window_size` | 4 | 非重叠窗口边长 |
| `num_heads` | `None` | 注意力头数；`None` 时自动选择并保证通道可整除 |
| `sampling_rate` | 2 | 金字塔每级的下采样/上采样倍率 |
| `max_levels` | 3 | 金字塔最大层数，实际层数还会受特征图尺寸限制 |

#### 4.2 `MultipoleAttention`

核心模块可以直接调用：

```python
MultipoleAttention(
    dim, window_size=4, num_heads=None,
    sampling_rate=2, max_levels=3, downsample="conv"
)
```

`downsample="conv"` 使用可学习的卷积和转置卷积，符合当前实现；`"avg"` 使用无
参数的平均池化和最近邻上采样，可在确定性训练对转置卷积报错时作为替代。但当前
`C2MANO` 尚未将 `downsample` 参数透传到 YAML，不能直接在 YAML 中切换。

推荐设置：

- P5=20×20 时使用 `window_size=4`，可获得 20→10→5 的多级特征；
- 插入 P3/P4 等较大特征图时，可尝试 `window_size=8`；
- `num_heads` 通常保持 `None`；
- 首轮实验保持 `e=0.5`，避免同时改变参数规模和注意力结构。

### 5. YAML 使用方法

#### 5.1 等价替换 `C2PSA`

```yaml
# 原始层
- [-1, 2, C2PSA, [1024]]

# C2MANO 变体
- [-1, 2, C2MANO, [1024]]
```

YAML 参数顺序为：

```text
[c2, e, window_size, num_heads, sampling_rate, max_levels]
```

示例：

```yaml
- [-1, 2, C2MANO, [1024]]                       # 全部默认
- [-1, 2, C2MANO, [1024, 0.25]]                 # 降低隐藏通道
- [-1, 2, C2MANO, [1024, 0.5, 8]]               # window_size=8
- [-1, 2, C2MANO, [1024, 0.5, 4, null, 2, 3]]  # 完整指定，null 表示自动 heads
```

当前完整配置中的关键层为：

```yaml
backbone:
  - [-1, 1, SPPF, [1024, 5]]
  - [-1, 2, C2MANO, [1024]]
```

文件位置：`ultralytics-main-new/0_orange_yaml/004_yolo11-seg-mano.yaml`。

#### 5.2 P3/P4/P5 尺度放置（不替换 C2PSA，当前实验采用）

保留 backbone 的 `C2PSA` 不动，把 `C2MANO` **追加在 neck 的 P3/P4/P5 输出之后**（基线 `001`
的 layer 16/19/22），再让 `Segment` 读精炼后的输出。追加在**末尾** → 不打断 PAN neck 内部
连线，layer 0–22 索引不变，`yolo11n-seg.pt` 的 backbone+neck 权重可**直接迁移**（无需层号平移）。

`C2MANO` 通道保持（`c1==c2`），首参用产出该尺度的 `C3k2` 的基础通道（256/512/1024）：

```yaml
# P3 尺度（005）：在 P3 输出(16)后加一层，Segment 改读 23
  - [16, 1, C2MANO, [256]]  # 23
  - [[23, 19, 22], 1, Segment, [nc, 32, 256]] # 24
# 三尺度联合（008）：
  - [16, 1, C2MANO, [256]]   # 23
  - [19, 1, C2MANO, [512]]   # 24
  - [22, 1, C2MANO, [1024]]  # 25
  - [[23, 24, 25], 1, Segment, [nc, 32, 256]] # 26
```

消融矩阵（单变量 = 放置尺度，其余同 `001`）：

| 实验 | YAML | C2MANO 位置 | Segment 输入 |
|---|---|---|---|
| E0 对照 | `001_yolo11-seg` | 无 | `[16,19,22]` |
| E-P3 | `005_yolo11-seg-mano-p3` | P3(16) | `[23,19,22]` |
| E-P4 | `006_yolo11-seg-mano-p4` | P4(19) | `[16,23,22]` |
| E-P5 | `007_yolo11-seg-mano-p5` | P5(22) | `[16,19,23]` |
| E-P345 | `008_yolo11-seg-mano-p345` | P3+P4+P5 | `[23,24,25]` |
| E1 旧 | `004_yolo11-seg-mano` | backbone C2PSA→C2MANO | `[16,19,22]` |

假设：P3（P3/8，最高分辨率、小目标尺度）对远处小柑橘增益最大；P345 为上限但最贵。
这些变体 append 在末尾，**直接用现有 `train_citrus_seg.py`**（backbone+neck 权重按键名正常迁移，
`Segment` 因 nc=1 本就重初始化），无需 HVI 那种层号平移驱动：

```powershell
python train_citrus_seg.py --model 0_orange_yaml/005_yolo11-seg-mano-p3.yaml   --pretrained yolo11n-seg.pt --name E_manoP3   --data 200orange_wuxi_seg.yaml
python train_citrus_seg.py --model 0_orange_yaml/006_yolo11-seg-mano-p4.yaml   --pretrained yolo11n-seg.pt --name E_manoP4   --data 200orange_wuxi_seg.yaml
python train_citrus_seg.py --model 0_orange_yaml/007_yolo11-seg-mano-p5.yaml   --pretrained yolo11n-seg.pt --name E_manoP5   --data 200orange_wuxi_seg.yaml
python train_citrus_seg.py --model 0_orange_yaml/008_yolo11-seg-mano-p345.yaml --pretrained yolo11n-seg.pt --name E_manoP345 --data 200orange_wuxi_seg.yaml
```

文件位置：`ultralytics-main-new/0_orange_yaml/005…008_*.yaml`。每层 C2MANO 默认 `n=1`（单块精炼），
可按需调成 `[16, 2, C2MANO, [256]]`。

### 6. 训练方法

先进入主代码目录并安装 editable 包：

```powershell
cd E:\mastercode\ultralytics-main-new
pip install -e .
```

Windows 本地训练：

```powershell
python train_citrus_seg.py `
  --model 0_orange_yaml/004_yolo11-seg-mano.yaml `
  --pretrained yolo11n-seg.pt `
  --name E1_mano `
  --data 200orange_wuxi_seg.yaml
```

`200orange_wuxi_seg.yaml` 当前指向 `E:/mastercode/data/orange_yolo`，包含：

```yaml
path: E:/mastercode/data/orange_yolo
train: train/images
val: val/images
test: test/images
nc: 1
names:
  0: orange_immature
```

服务器 Linux 环境中，将主代码和数据复制到服务器对应路径后，把 `--data` 改为
服务器上的 YAML 文件路径，把 `--project` 改为服务器结果目录。服务器命令行可使用：

```bash
python train_citrus_seg.py \
  --model 0_orange_yaml/004_yolo11-seg-mano.yaml \
  --pretrained yolo11n-seg.pt \
  --name E1_mano \
  --data /path/to/orange_yolo/data.yaml \
  --project /path/to/results
```

正式训练前先运行 1～3 个 epoch 的 smoke test，确认 YAML、数据路径和显存均正常：

```powershell
python train_citrus_seg.py `
  --model 0_orange_yaml/004_yolo11-seg-mano.yaml `
  --pretrained yolo11n-seg.pt `
  --name E1_mano_smoke `
  --data 200orange_wuxi_seg.yaml `
  --epochs 3
```

### 7. Python 直接构建和调用

```python
import torch
from ultralytics import YOLO
from ultralytics.nn.modules import C2MANO, MultipoleAttention

model = YOLO("0_orange_yaml/004_yolo11-seg-mano.yaml")
model.info()

block = C2MANO(256, 256, n=1)
y = block(torch.randn(1, 256, 20, 20))
assert tuple(y.shape) == (1, 256, 20, 20)

attention = MultipoleAttention(128, window_size=4, max_levels=3)
y2 = attention(torch.randn(1, 128, 80, 80))
assert tuple(y2.shape) == (1, 128, 80, 80)
```

### 8. 注册和迁移检查

将模块迁移到另一份 Ultralytics 代码时，必须同步检查以下四处：

1. `ultralytics/nn/modules/mano.py`：模块实现；
2. `ultralytics/nn/modules/__init__.py`：导入并加入导出列表；
3. `ultralytics/nn/tasks.py`：导入 `C2MANO`；
4. `ultralytics/nn/tasks.py` 的 `parse_model()`：加入 `base_modules` 和
   `repeat_modules`。

缺少第 3 或第 4 步时，通常会出现 YAML 解析或未知模块错误。迁移到服务器时只复制
主代码中实际需要的模块、注册文件、YAML、训练脚本和数据配置；不要修改或覆盖
`E:\mastercode\1_SEVER`。

### 9. 当前验证记录与注意事项

当前记录的 `004_yolo11-seg-mano` 结果为：

| 模型 | 参数量 | GFLOPs | 验证 |
|---|---:|---:|---|
| `001` baseline（C2PSA） | 2,876,848 | 10.5 | — |
| `004` C2MANO | 2,988,851 | 11.2 | 构建、640/512/608 前向、反向通过 |

这组数字只能作为当前环境下的历史记录。正式论文实验必须在相同数据划分、输入尺寸、
初始化、优化器、学习率、epoch、seed 和评估协议下重新记录。不要覆盖已经完成的
实验目录，建议使用 `E1_mano_日期` 或连续编号命名。

如果确定性训练环境对 `ConvTranspose2d` 报错，需要先修改源码，把 `downsample="avg"`
从 `MultipoleAttention` 逐层透传到 `C2MANO`，再在 YAML 中使用：

```yaml
- [-1, 2, C2MANO, [1024, 0.5, 4, null, 2, 3, avg]]
```

修改后必须重新进行模型构建、前向、反向和 FLOPs 检查，再开始正式训练。

---

## HVIEnhance：区域门控低光照增强前端

> 模块文件：`ultralytics-main-new/ultralytics/nn/modules/hvi_enhance.py`
> 示例配置：`ultralytics-main-new/0_orange_yaml/010_yolo11-seg-hvi.yaml`
> 训练脚本：`ultralytics-main-new/train_citrus_seg_hvi.py`（带层号平移迁移）
> 自检脚本：`ultralytics-main-new/test_hvi_enhance.py`
> 适用任务：YOLO11 RGB 柑橘幼果实例分割（远处弱光/低对比目标）
> 来源论文：*HVI: A New Color Space for Low-light Image Enhancement*
> （HVI-CIDNet，CVPR 2025，arXiv:2502.20272，[代码](https://github.com/Fediory/HVI-CIDNet)）

### 1. 模块用途

`HVIEnhance` 把 HVI-CIDNet 论文的核心贡献 —— **HVI 色彩空间变换**（极化 Hue/Saturation
+ 可学习强度塌缩 `density_k`）—— 移植为一个即插即用的 Ultralytics 模块，作为 **layer 0**
在原始 3 通道输入图上做低光照增强，输出同分辨率的 3 通道增强图，再送入原 YOLO11-seg 主干。

设计要点：

- **只保留论文中真正有用的部分**：HVI 变换负责把颜色与亮度解耦，压掉 sRGB/HSV 在暗区/红区
  的伪影；论文里昂贵的双分支 CIDNet（交叉注意力 LCA，1.88M/7.57 GFLOPs）被替换成一个
  微型「降采样 → 门控卷积 → 上采样」残差增强子（`IELBlock`），前端很轻。
- **不需要成对低光/正常光 GT**：整个模块可微，直接由下游分割损失端到端训练。
- **区域感知门控（自监督）**：一个极小的门控头预测逐像素门控 `g∈[0,1]`，增强以
  `hvi + g·delta` 的方式施加。门控只由任务损失驱动，会自动聚焦到「增强能帮上检测」的地方
  （远处、暗、低对比的柑橘）；**暗区先验** `g *= (1 - I)` 进一步把增强偏置到低光区域，
  让已经明亮的区域保持不变。这样在**不引入任何额外标注**的前提下实现「先感知目标区域、
  只在该区域增强」。

**适用边界（务必如实理解）**：HVI 针对的是**低光照 / 颜色退化**，能改善暗处柑橘的可见性与
色彩纹理；它**不直接解决**低分辨率 / 模糊（那是超分 / 去模糊，另有 real-esrgan）。
「省算力」体现在两点：①增强子做得很轻、且在**降采样分辨率**处理；②门控让增强在效果上
聚焦关键区域。**稠密卷积下，算门控与残差本身不因门控而省 FLOP**；推理期按门控跳过 `g≈0`
区域的稀疏加速属于可选的后续工作，本模块不做此承诺。

### 2. 模块结构

```text
HVIEnhance
├── RGBHVI.HVIT          # sRGB([0,1],RGB序) → [H,V,I]，可学习 density_k
├── stem: Conv3x3+SiLU   # 3 → base
├── (avg_pool ↓down)     # 降采样省算力
├── blocks: n × IELBlock # 门控深度卷积 FFN（纯卷积，无 einops/注意力）
├── (nearest ↑down)      # 上采样回原分辨率
├── res_head: Conv3x3    # base → 3 残差 delta（零初始化 → 初始恒等）
├── gate_head: Conv3x3   # base → 1，sigmoid 门控 g；暗区先验 g *= (1-I)
│      out_hvi = hvi + g·delta
└── RGBHVI.PHVIT         # [H,V,I] → sRGB([0,1])
```

**确定性重写（关键）**：参考实现 `net/HVI_transform.py::RGB_HVI` 用布尔掩码散射赋值
（`hue[mask]=...`，18 处）和双线性上采样，这些在 CUDA 上有**非确定性反向核**，在
`deterministic=True`（本项目 FIXED 协议默认开启）下会报错或不可复现。本模块用
`torch.where` + `amax`/`amin` + **最近邻上采样**逐一重写，保证 bit 级可复现，并在
`test_hvi_enhance.py` 中与参考公式做数值对齐（`max|Δ| < 1e-5`）。

### 3. 输入输出与限制

| 项目 | 说明 |
|---|---|
| 输入 | `[B, 3, H, W]`，**RGB、[0,1]**（YOLO 在 layer 0 已 `/255` 且默认 `bgr=0` → RGB） |
| 输出 | `[B, 3, H, W]`，增强后的 RGB 图，通道数与空间尺寸不变 |
| 通道约束 | 强制 `c1 == c2 == 3`（图像前端）；**不进 `base_modules`**，避免 width 缩放把 3 变成 8 |
| 位置约束 | 必须是 **layer 0**（在主干之前，作用于原图）；插入后后续层号整体 +1 |
| 初始行为 | 残差头零初始化 → 初始 ≈ 恒等，不扰动下游迁移学习 |
| 依赖 | 纯 PyTorch，无 einops / huggingface_hub 等新依赖 |

### 4. 参数说明

```python
HVIEnhance(
    c1=3, c2=3, base=16, blocks=2,
    down=2, expansion=1.0, gate=True, dark_prior=True
)
```

| 参数 | 默认值 | 说明 |
|---|---:|---|
| `c1` | 3 | 输入通道，由 `parse_model()` 填 3 |
| `c2` | 3 | 输出通道，固定 3（保持给主干的图像通道） |
| `base` | 16 | 增强子宽度，越小越省算力 |
| `blocks` | 2 | `IELBlock` 堆叠次数 |
| `down` | 2 | 增强子内部降采样倍率（省算力）；1 = 全分辨率处理 |
| `expansion` | 1.0 | `IELBlock` 隐藏通道比例（论文用 2.66，这里为省算力取 1.0） |
| `gate` | True | 是否启用学习门控（区域感知） |
| `dark_prior` | True | 门控是否叠加暗区先验 `g *= (1 - I)` |

省算力建议：`down=4` / `base=8` 可进一步减半开销；消融门控时设 `gate=False` 或
`dark_prior=False`。

### 5. YAML 使用方法

`HVIEnhance` 作为 **layer 0** 插在主干最前。YAML 参数顺序（`c1/c2` 由解析器自动补，
不在 YAML 写）：

```text
[base, blocks, down, expansion, gate, dark_prior]
```

示例：

```yaml
backbone:
  - [-1, 1, HVIEnhance, [16, 2]]            # 默认 down=2,expansion=1.0,gate/dark_prior=True
  - [-1, 1, Conv, [64, 3, 2]]              # 1-P1/2 …（原主干整体下移一层）
# 消融示例
  - [-1, 1, HVIEnhance, [8, 1, 4]]          # 更省：base=8,blocks=1,down=4
  - [-1, 1, HVIEnhance, [16, 2, 2, 1.0, False, False]]  # 关掉门控与暗区先验
```

**关键规则 —— 插入 layer 0 会使后续层号整体 +1**，必须同步把 head 里所有**绝对**
`from` 索引 +1（`-1` 相对引用不变）。以 `001_yolo11-seg.yaml` 为基线，对应改动：

| 位置 | 基线索引 | HVI 版索引 |
|---|---|---|
| Concat cat backbone P4 | `[[-1, 6]]` | `[[-1, 7]]` |
| Concat cat backbone P3 | `[[-1, 4]]` | `[[-1, 5]]` |
| Concat cat head P4 | `[[-1, 13]]` | `[[-1, 14]]` |
| Concat cat head P5 | `[[-1, 10]]` | `[[-1, 11]]` |
| Segment 输入 | `[[16, 19, 22]]` | `[[17, 20, 23]]` |

完整文件见 `ultralytics-main-new/0_orange_yaml/010_yolo11-seg-hvi.yaml`。

### 6. 训练方法

先安装 editable 包（与其它模块一致）：

```powershell
cd E:\mastercode\ultralytics-main-new
pip install -e .
```

**迁移学习注意**：插入 layer 0 后各层的 state_dict 键名（`model.<i>.*`）整体 +1，
直接 `YOLO(yaml).load(yolo11n-seg.pt)` 会按键名几乎匹配不到、退化成从头训练，导致与基线
不公平。因此提供 `train_citrus_seg_hvi.py`，它**复用 `train_citrus_seg.py` 的 FIXED 协议**
（不改动原基线驱动），并在加载时把预训练 `model.<i>.*` 映射到 `model.<i+1>.*` 再匹配 ——
这样 backbone/neck/head 100% 迁移，只有新增的 layer 0 随机初始化。

Windows 本地训练：

```powershell
# 1~3 epoch smoke test（随机初始化，先验证管线跑通）
python train_citrus_seg_hvi.py `
  --model 0_orange_yaml/010_yolo11-seg-hvi.yaml `
  --name E_hvi_smoke --epochs 3

# 正式训练（把 COCO 权重公平迁移进平移后的层）
python train_citrus_seg_hvi.py `
  --model 0_orange_yaml/010_yolo11-seg-hvi.yaml `
  --pretrained yolo11n-seg.pt `
  --name E_hvi
```

`--data` 默认沿用 `train_citrus_seg.py` 的 FIXED 数据集，也可显式指定
（如 `--data 200orange_wuxi_seg.yaml`）。其余超参（AdamW、lr0=0.01、amp=0、
deterministic=True、imgsz=640…）与基线完全一致，保证「只有架构不同」的干净消融。

服务器 Linux 环境：把主代码与数据放到服务器对应路径后，`--data`/`--project` 改成服务器
上的路径即可，命令与上面一致（正斜杠、反引号换成反斜杠续行）。**不要改动服务器上已有的
路径常量**（见第 8 节）。

### 7. Python 直接构建与调用

```python
import torch
from ultralytics import YOLO
from ultralytics.nn.modules import HVIEnhance, RGBHVI

# 单独用增强前端
m = HVIEnhance(3, 3, base=16, blocks=2)
x = torch.rand(2, 3, 640, 640)          # RGB in [0,1]
y = m(x)
assert tuple(y.shape) == (2, 3, 640, 640)

# 单独用色彩变换（可微、确定性）
t = RGBHVI()
hvi = t.HVIT(x)                         # [B,3,H,W] = [H,V,I]
rgb = t.PHVIT(hvi)                      # 往返 ≈ 恒等

# 整模型
model = YOLO("0_orange_yaml/010_yolo11-seg-hvi.yaml")
model.info()
```

自检：`python test_hvi_enhance.py` 会验证①HVIT 与参考公式一致、②往返恒等、
③初始恒等、④前反向与 `density_k` 梯度、⑤确定性、⑥010 模型构建 + 前向 + 参数量。

### 8. 注册与迁移检查

新增本模块共改动 **4 处**（与 C2MANO 相同的机制，但注册方式不同）：

1. `ultralytics/nn/modules/hvi_enhance.py`：模块实现（`RGBHVI` / `IELBlock` / `HVIEnhance`）；
2. `ultralytics/nn/modules/__init__.py`：导入并加入 `__all__`；
3. `ultralytics/nn/tasks.py`：顶部导入 `HVIEnhance`；
4. `ultralytics/nn/tasks.py` 的 `parse_model()`：**专用 `elif m is HVIEnhance` 分支**
   （**不要**加入 `base_modules`）：

   ```python
   elif m is HVIEnhance:
       c1 = ch[f]
       c2 = c1          # 通道保持 3 → 3，不受 width 缩放
       args = [c1, c2, *args]
   ```

   原因：`base_modules` 路径会执行 `c2 = make_divisible(min(c2,max)*width, 8)`，
   在 `n` 尺度（width=0.25）下会把 `c2=3` 放大成 8，破坏「输出 3 通道图像」的约定。
   这与 `WCAF` / `ShuffleV2Stem_Depth` 等「不缩放」模块的写法同理。

**迁移到服务器 `1_SEVER`**：只复制 `hvi_enhance.py`，并在服务器代码里做同样的 4 处注册，
再加入 `010_yolo11-seg-hvi.yaml` 与 `train_citrus_seg_hvi.py`。**绝不修改 `1_SEVER` 中任何
硬编码的服务器路径**（`/data/sxq/...`、`SERVER_*` 等）——服务器代码是从服务器原封不动拷回的，
路径改错会导致服务器上训练找不到数据 / 权重。迁移前先扫一遍路径常量，只做「加模块」不做
「改路径」。

### 9. 当前验证记录与注意事项

| 检查项 | 状态 |
|---|---|
| HVIT vs 参考公式数值对齐 | ✅ 通过 `max|Δ| = 0.000e+00`（确定性重写与参考逐位一致） |
| 往返 PHVIT∘HVIT | ✅ 通过 `mean|Δ| = 3.8e-08`（近乎完美可逆） |
| 初始恒等（残差头零初始化） | ✅ 通过 `mean|Δ| = 3.8e-08`，输出范围 [0,1] |
| 前反向 + density_k 梯度 | ✅ 通过 `grad = 2.28e-02` |
| 确定性（两次前向一致） | ✅ 通过 `max|Δ| = 0.000e+00` |
| 010 模型构建 | ✅ 通过 221 层 / **2,880,789** 参数（基线 001 = 2,876,848，**仅 +3,941**） |
| GFLOPs | 训练 / `model.info()` 时打印（基线 10.5，预计约 +1 GFLOP） |
| 1~3 epoch smoke test | ⏳ 待执行 —— 本机 `data/test` 不存在，用 `--data 200orange_wuxi_seg.yaml`（`data/orange_yolo`，676 训练图） |

> 自检通过环境：`yolo` conda 环境（`E:\AppInstallion\0_4_annaconda\envs\yolo`）。若要增大增强容量，
> 可把 YAML 改成 `[-1, 1, HVIEnhance, [32, 3]]`（base=32、blocks=3）再重测参数 / FLOPs。

正式论文实验必须在与基线 `001` 相同的数据划分、输入尺寸、初始化、优化器、学习率、epoch、
seed 和评估协议下记录，只让「有无 HVIEnhance / 门控 / 暗区先验」作为单变量。不要覆盖已有实验
目录，建议 `E_hvi_日期` 或连续编号命名。

### 10. 服务器 `1_SEVER` 迁移记录

已把模块迁移到 `1_SEVER/code/ultralytics-main-new/`，与本地逐字节一致，**未改动任何服务器路径**：

| 文件 | 操作 |
|---|---|
| `ultralytics/nn/modules/hvi_enhance.py` | 新增（与本地相同） |
| `ultralytics/nn/modules/__init__.py` | 仅加导入 + `__all__` 三个符号 |
| `ultralytics/nn/tasks.py` | 仅加顶部导入 + `elif m is HVIEnhance` 分支 |
| `0_orange_yaml/010_yolo11-seg-hvi.yaml` | 新增 |
| `train_citrus_seg_hvi.py` | 新增（层号平移迁移驱动） |
| `test_hvi_enhance.py` | 新增（自检脚本） |

- 迁移前已确认服务器 fork 与本地**同版本、同结构**：`__init__.py` 的 mano 块、`tasks.py` 的
  `WCAF`/`ShuffleV2Stem_Depth` elif、基线 `001_3_yolo11-seg.yaml` 结构均与本地一致，故 4 处
  注册的 anchor 和 010 YAML 可原样套用。
- **零路径改动**：服务器 `train_citrus_seg.py` 里的 `DATA`/`PROJECT` 常量原封未动；本模块的
  驱动 import 它们作默认值，服务器上用 `--data`/`--project` 显式覆盖到 `/data/sxq/...` 即可。
  用 `git diff` 查看 `1_SEVER` 只会看到「加导入 / 加注册 / 加新文件」，无任何路径行改动。
- **服务器侧验证（待执行）**：撰写时本机执行工具不可用，服务器副本尚未运行时验证。在服务器上跑：

  ```bash
  cd <server>/code/ultralytics-main-new
  python test_hvi_enhance.py          # 6 项自检
  python train_citrus_seg_hvi.py --model 0_orange_yaml/010_yolo11-seg-hvi.yaml \
      --name E_hvi_smoke --epochs 3 --data /data/sxq/.../你的data.yaml --project /data/sxq/你的结果目录
  ```

---

## HVIEnhance + C2MANO 组合模型（011，ours-full）

把两项改进叠加成一个模型：**layer 0 的 HVIEnhance 低光前端** + **P3/P4/P5 的 C2MANO 精炼**。
文件：本地 `ultralytics-main-new/0_orange_yaml/011_yolo11-seg-hvi-mano.yaml`，服务器同名已同步。

- HVIEnhance 在 layer 0 使后续层整体 +1；C2MANO 追加在**平移后**的 neck P3/P4/P5 输出
  （`17/20/23`）之后，`Segment` 读 `[24,25,26]`。C2MANO 通道保持（`c1==c2`，64/128/256）。
- **迁移**：用 `train_citrus_seg_hvi.py`（层号平移 `shift=1`）—— backbone+neck 从 COCO 迁移，
  HVIEnhance / 3×C2MANO / Segment 为新层（随机初始化 / 按 nc=1 重初始化）。

```powershell
python train_citrus_seg_hvi.py --model 0_orange_yaml/011_yolo11-seg-hvi-mano.yaml `
  --pretrained yolo11n-seg.pt --name E_hvi_manoP345 --data 200orange_wuxi_seg.yaml
```

**完整消融阶梯**（每步只加一项改进，便于论文分解各自与联合的边际贡献）：

| 台阶 | YAML | 改进 | 迁移驱动 |
|---|---|---|---|
| 基线 | `001_yolo11-seg` | — | `train_citrus_seg.py` |
| +HVI | `010_yolo11-seg-hvi` | 低光前端 | `train_citrus_seg_hvi.py` |
| +C2MANO | `008_yolo11-seg-mano-p345` | P3/P4/P5 注意力 | `train_citrus_seg.py` |
| 全量 | `011_yolo11-seg-hvi-mano` | HVI + C2MANO | `train_citrus_seg_hvi.py` |

> 构建验证（Bash 恢复后或本机自行运行）：
> `python -c "from ultralytics import YOLO; YOLO('0_orange_yaml/011_yolo11-seg-hvi-mano.yaml').info()"`
> 应正常打印层数 / 参数量，不报 `unknown module`。
