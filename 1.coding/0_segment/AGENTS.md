## Agent skills

### Issue tracker

Issues 使用 GitHub Issues 进行追踪，通过 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用中文标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局 — `CONTEXT.md` + `docs/adr/` 位于 `1.coding/0_segment/` 目录下。详见 `docs/agents/domain.md`。
